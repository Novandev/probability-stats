"""
Tha additive low of probability

This law gives you the sum probabilities of both independent depended event that may be exclusive or mutually exlusive

"""





def additive_probability_dependent(a=0,b=0, pr):
    """
    This aasumes the prob is nto complemented
    
    """
    if a == 0 or b == 0 :
        assert (a ==0 or b == 0), 
